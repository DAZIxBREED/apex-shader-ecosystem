# Apex Packages

Each `com.dazi.apex.*` folder is an independent Unity UPM package. Pin all selected packages to the same release tag. Install Core and SpectraOverdrive first, followed by the visual packages, Tools, and optional Examples. See [`Documentation/INSTALLATION.md`](../Documentation/INSTALLATION.md) for Git URLs and a complete project-manifest example.
